const sum1 = (a: number, b: number): number => a + b;

describe('<Map />', () => {
  test('dev mode?', () => {
    expect(process.env.NODE_ENV).toMatch("development");
  });
  test('latlongthere?', () => {
    
  });
  
});
